# VaR_GreenHill_Capital
A Python script to calculate the 1-day historical Value at Risk (VaR) and Conditional Value at Risk (CVaR) for an investment club's multi-asset portfolio.

Ready-To-Run, just need to install package using the latest version of Python and the command "python -m pip install yfinance numpy pandas" in your terminal.
